name: agora_rtc_sample
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  agora_rtc_engine: ^4.0.6
  flutter_state_notifier: ^0.7.0
  freezed_annotation: ^0.14.2
  hooks_riverpod: ^0.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  build_runner: ^2.0.6
  freezed: ^0.14.2

flutter:

  uses-material-design: true